import { PageContainer } from '@ant-design/pro-components';

const Workflow: React.FC = () => {
  return <PageContainer header={{ title: '' }} breadcrumb={{}}>Coming Soon</PageContainer>;
};

export default Workflow;
